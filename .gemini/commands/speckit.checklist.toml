description = "Spec Kit step: generate a requirements-quality checklist for a chosen domain."
prompt = """
This repo's Spec Kit workflow is plain Markdown; nothing needs to be installed.
1. Read AGENTS.md completely (it is short) and follow its "Session start" section.
2. Open .claude/commands/speckit.checklist.md, read it completely and follow it exactly,
   ignoring its YAML front matter and treating the text below as $ARGUMENTS.
3. Finish with AGENTS.md "Session end and hand-off" (log entry with Handoff, commit).

$ARGUMENTS:
{{args}}
"""
