description = "Read-only consistency and constitution check of spec, plan and tasks."
prompt = """
Read AGENTS.md (sections 0 and 4) if you have not already in this session.
Then open .claude/commands/speckit.analyze.md and follow its instructions exactly,
ignoring its YAML front matter and treating the text below as $ARGUMENTS.

$ARGUMENTS:
{{args}}
"""
