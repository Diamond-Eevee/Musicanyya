description = "Resume where the last agent stopped: next chunk of work, then hand-off."
prompt = """
Read AGENTS.md (sections 0, 4, 5 and 6) if you have not already in this session.
Then open .claude/commands/speckit.continue.md and follow its instructions exactly,
ignoring its YAML front matter and treating the text below as $ARGUMENTS.

$ARGUMENTS:
{{args}}
"""
