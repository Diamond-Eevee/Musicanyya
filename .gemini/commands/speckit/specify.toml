description = "Create a feature spec (WHAT/WHY) on a new NNN-name branch."
prompt = """
Read AGENTS.md (sections 0 and 4) if you have not already in this session.
Then open .claude/commands/speckit.specify.md and follow its instructions exactly,
ignoring its YAML front matter and treating the text below as $ARGUMENTS.

$ARGUMENTS:
{{args}}
"""
