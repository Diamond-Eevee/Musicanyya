# Musicanyya - GitHub Copilot

Follow `AGENTS.md` in the repository root: it is the canonical guide for all agents (session start, workflow steps,
task claims, hand-off log, quality gates). The non-negotiable rules are in `.specify/memory/constitution.md`.

- "continue" means: follow `.claude/commands/speckit.continue.md` (plain Markdown; ignore its YAML front matter).
- "implement" means: follow `.claude/commands/speckit.implement.md`.
- Your agent id is `copilot-<model>`.
