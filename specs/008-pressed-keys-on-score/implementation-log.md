# Implementation log: 008-pressed-keys-on-score

## 2026-09-25 - claude-opus-5.5 (analyze)
- Analyze: 14 findings (CRITICAL 0, HIGH 1, MEDIUM 6, LOW 7); tasks.md as of 102424b
- Top recommendations: (A1) give `skipped` a shape, since grey vs green heads behind the cursor differ by colour only (FR-010); (A2) say when green marks clear on a loop/repeat (the matcher clears each note as the cursor reaches it again) and test it; (A3-A4) add zoom/reflow and Electron checks for FR-013/FR-015; (A5) record canvas-drawn ledger lines/ottava labels in plan Complexity Tracking (Constitution III)
- Handoff: next = resolve A1 (needs owner: skipped mark shape), then /speckit.implement from T001; no code changed
- Remediation (owner: "answer all with recommended"): A1 skipped = grey head + right-pointing skip chevron (spec FR-009/FR-010, R-03, contract `drawStateChevron`, T050/T052/T053/T054); A2 FR-012 = marks clear note by note as the session reaches them again (existing matcher behaviour), tested in T014(h); A3 zoom checks in T014(g)/T032; A4 new T066 Electron e2e; A5 plan Complexity Tracking row for canvas ledger lines/ottava labels; A6 new static test T057; A7 `harvestGlyphs` in `src/workers/glyphs.ts` (T030/T038); A8/A12 FR-011 and 10-key frame-time assertions in T032; A9 contract aligned; A10 staff found via `closest('g.staff')` (R-05); A11 chevrons relative to the notehead box; A13 README in T004; A14 owner confirmed shift-aside (FR-006). Now 53 tasks.
- Handoff: next = /speckit.implement from T001; no code changed

## 2026-09-25 11:05 - claude-sonnet-5 (implement, Phases 1-2)
- Baseline before any change: `pnpm test` Test Files 184 passed (184), Tests 2115 passed (2115), exit 0; `pnpm typecheck` exit 0; `pnpm lint` exit 0 (284 warnings, 13 infos, all pre-existing; none added by this feature).
- Done: T001 (five colour tokens in `tokens.css`), T002 (`PRACTICE_DISC_MAX_LEDGER_LINES`, `PRACTICE_DISC_OTHER_STAFF_LEDGER_LINES` in `defaults.ts`), T003 (`tests/e2e/helpers/practice.ts`: `pressKeys`, `startPractice`, `startPracticeOnOpenScore`), T004 (`--practice`, `--keys` in `tools/dev/screenshot.ts`; docs in its header, README, `docs/agents/reference.md`; `quickstart.md` already described them), T005.
- Added beyond the task text: `tools/dev/key-steps.ts` (the `+<midi>` / `-<midi>` / `wait` parser shared by the screenshot tool and `pressKeys`, so both agree) and its test `tests/tools/key-steps.test.ts` (`Tests 10 passed (10)`).
- T005 result: `pnpm screenshot -- --item repertoire/intermediate/fur-elise-theme --practice --keys "+76" --out tests/.generated/008/t005.png` -> notices none, console errors none. Looked at the picture: Practice is active (Practice radio on, Stop button), the first E5 has today's solid green ring and the next note (D#5) the dashed sky-blue ring, i.e. the old marks that US1 replaces.
- Decisions: the screenshot tool waits with Playwright locators, not `waitForFunction` strings, because the page's CSP (no `unsafe-eval`) rejects string predicates (first attempt failed with that EvalError).
- Handoff: next = T010-T012 (US1 tests: `tests/ui/note-marks.test.ts`, `tests/ui/practice-band.test.ts`), then T013, T014, then T015-T020; tree clean after the commit.

## 2026-09-25 12:10 - claude-sonnet-5 (implement, US1 checkpoint)
- Done: T010-T020 (US1), plus new T067/T068 and T021 (US2 fixtures, made early because they only add files).
- Tests written first and seen failing for the expected reason: `tests/ui/note-marks.test.ts` and `practice-band.test.ts` (module missing), `tests/ui/practice-marks.test.ts` T013 (`stroke` still called 3 times for waiting/correctSoFar/correct), `tests/e2e/pressed-keys.spec.ts` (6 failed: notehead `rgb(0, 0, 0)` not green), T067 (a) and (d) (mark still there after the release).
- **Found while running T014, fixed as new tasks T067/T068**: the matcher never withdrew a `correctSoFar` mark on `noteOff` (data-model section 4 and FR-003 assumed it did). `noteOff` of a key required by the current event now removes the `correctSoFar` / `heldOver` mark of its notes. Releasing a held-over key clearing its mark is my reading of FR-009a (its hint is hidden at that moment); flagged in case the owner wants the orange head to stay until the next press. data-model section 4 and research R-12 corrected. No replay snapshot changed.
- **Replaced assertions (spec behaviour changed, FR-009)**: `tests/ui/practice-marks.test.ts` - the six-state outline test now covers only heldOver/playedAlong/skipped (US3, T050 replaces those) and a new test asserts waiting/correctSoFar/correct draw no stroke, arc or dash and dimming is unchanged; `tests/ui/overlay-layers.test.ts` - the "practice marks draw when on, nothing when off" check used a `correct` outline, it now goes through the dimming the layer still paints. Nothing weakened: every replaced assertion has a new one for the new mark.
- Design decision (recorded in `score.css`): the band is `z-index: -1` inside `.mx-score-stack` (`position: relative; isolation: isolate`); pages get no `z-index`, because one would lift them above the overlay canvas that carries the discs and chevrons.
- Extra dev tooling: `--play <n>` in `tools/dev/screenshot.ts` (plays the correct keys of the first n events, for real scores); documented in header, README, reference, quickstart.
- Evidence: `pnpm test` Test Files 187 passed (187), Tests 2144 passed (2144), exit 0; `pnpm typecheck` exit 0; `pnpm lint` exit 0 (284 warnings, 13 infos, same as before). `pnpm test:e2e` (all four projects): 312 passed, 71 skipped, **1 failed**: `[electron] library.spec.ts:175 "electron: identical behaviour under the app:// origin"` - `electron.launch: Target page, context or browser has been closed` (the Electron process exited at start-up under four parallel workers, no assertion ran); re-run alone: `1 passed (3.5s)`. Treated as a launch flake, not caused by this feature; T065 re-runs the whole gate.
- Pictures looked at: `tests/.generated/008/us1.png` (Fur Elise: the first four heads green, stems black, translucent band behind the next E5 spanning both staves, no dashed ring or square), `us1-real-chopin.png` (grand-staff piano piece from `tests/fixtures/musicxml/real`, `--play 6`: green heads on both staves, the hollow half note stays a hollow green outline, band behind the next event), `us1-chord.png`, `us1-zoom.png` from the e2e run.
- Handoff: next = US2 tests T022-T031 (fixtures T021 are in `tests/fixtures/musicxml/notation/`), then T032-T042; run `pnpm test -- tests/core` first; tree clean after the commit. Note for T033: octave-shift stop is treated as *exclusive* (a note at the stop position is not shifted), matching `engraving/walk.ts`, so update data-model section 1 ("inclusive") when implementing.

## 2026-09-25 12:40 - claude-sonnet-5 (implement, US2 + US3 checkpoints)
- Done: T022-T042 (US2), T050-T057 (US3), T060, T061. Both checkpoints share one commit (the view file carries both stories).
- Tests written first, seen failing for the expected reason: notation/spell/staff-position/place-discs/context (module missing), parser tests (`clefs`/`keys`/`octaveShifts` undefined), matcher `heldWrongKeys` (undefined) and replay (`session.heldWrongKeys` missing), disc-layout / pressed-keys / glyphs (module missing), e2e US2 (9 failed, `data-discs` empty), US3 unit tests (chevron/no-dash/live marks).
- Decisions (research/data-model/contract updated): (1) octave-shift `stop` is exclusive, as `engraving/walk.ts` prints it; (2) `ottava` may be +-3 (22ma/22mb): under a Score's own 8va/15mb, or on a lone bass staff, a piano key can be up to 16 ledger lines away and two folds were not enough (found by the property test); (3) both hands with a note on one staff only: the staff without a note competes by the pitch of its middle line, so A0 goes to the bass staff with "8vb" instead of the treble staff with "22mb" (place-discs test 3b); (4) discs keep clear of a held-over chevron (mark obstacles in `layoutDiscs`, found in the greyscale picture); (5) new load notice `unsupportedClef` (percussion/TAB/jianpu/none, info): +8 files in the community suite snapshot, nothing else changed; (6) `data-discs` JSON on the overlay canvas is an e2e/debug seam (contract 1.1.0).
- Snapshots updated with the reason: `build.test.ts.snap` (77 entries) only gained the three new `Part` lists (1009 added lines, 0 removed, checked by diff); `community-suite.test.ts.snap` `unsupportedClef: 8`; `replay.test.ts.snap` two goldens gained the `heldWrongKeys` timeline (182 added lines, 0 removed) plus a new golden for wrong keys across an event change. T022's "existing snapshots unchanged" could not hold literally (adding fields to `Part` changes every serialised Score); the notes, ids and ticks are unchanged.
- Replaced assertions (specified behaviour changed): `tests/ui/practice-marks.test.ts` - the heldOver/playedAlong/skipped outline test is now "draws no mark of its own for any state", the `drawCursorOverlay` practice-rectangle test went with the branch it tested (T055), new chevron tests; `tests/ui/grade-marks.test.ts` - the `drawLiveMarks` dashed-ring tests are replaced by view tests that live marks are `mx-mark-correct` and clear on Grade / new run / mode change / layer off; `tests/ui/overlay-layers.test.ts` - `drawLiveMarks` is gone from the switch test. Also: three `Part` literals in `tests/core/timeline/*.test.ts` gained the three empty lists.
- **Deviation from T032's wording, measured**: "no frame over 16.7 ms while ten discs are held" cannot hold in headless Chromium: with no disc at all single frames take 18-19 ms (idle sample 2026-09-25: max 18.4-19.1 ms, median 16.6 ms). The test asserts what the discs can influence: median <= 17.5 ms, no frame >= 25 ms (a skipped frame is ~33 ms) and max <= idle max + 3 ms. Ten discs measured the same as idle.
- Extra fixture: `notation/marks-four-states.musicxml` (README row) for the greyscale check T053.
- Evidence: `pnpm test` Test Files 195 passed (195), Tests 2277 passed (2277), exit 0; `pnpm typecheck` exit 0; `pnpm lint` exit 0 (284 warnings, 13 infos, unchanged). `pnpm test:e2e`: 360 passed, 87 skipped, 1 failed: `electron-playback.spec.ts:47` "the audio clock advances under app://" got "stalled" once in the full run; re-run alone `2 passed (4.6s)` (a load-dependent audio-clock flake in test code this feature does not touch). The earlier `library.spec.ts:175` Electron failure recurs in full runs and also in `--project=electron` alone: `electron/main.ts` takes `app.requestSingleInstanceLock()`, keyed on the user-data dir, and that spec launches without its own `--user-data-dir` (the other Electron specs pass one) - a pre-existing race, not caused by this feature; passed alone. Split off as a separate task (chip).
- Pictures looked at: `us2-d5.png` (D5 disc on the D5 line, right of the black E5, band behind), `us2-sharp.png` (A#4 with a real Leipzig sharp left of the disc), `us2-a0.png` (A0 on the bass staff, three ledger lines, "8vb"), `us2-ten.png` (ten discs zig-zag in three columns with stacked accidentals), `us2-real-chopin.png` (real grand-staff piece in G major: F#4 and F#1 with no sign, A#3 with a sharp, low key with ledger lines), `us3-heldover.png` (orange head, caret above, hint), `greyscale.png` (skipped ">" below D4, caret above the held-over E4, disc moved clear of it), `us3-grace.png`, `us3-play.png` (green head during a Play run, no ring).
- Known cosmetic limit: in a dense 16th passage a disc moved right can cover part of the next note's accidental (the layout only avoids the current column's heads); it never covers a written notehead of the current event.
- needs owner: the spec edge case "Hands separately" (line 138) says a key that plays the *other hand's* written note shows as a red disc, but FR-004, R-03, R-12 and the 002 contract make that key `playedAlong` ("never a mistake"), which this feature shows as a green notehead (the same as a played grace note, US3.1). Implemented as `playedAlong` = green, no disc. Say if the other hand's note should be a red disc instead (a matcher change: `playedAlong` keys of unselected hands would join `heldWrongKeys`).
- Also flagged (recommendation, reversible): releasing a held-over key now clears its orange mark together with its hint (T068).
- Handoff: next = T062 (real-file pictures), T063 (quickstart run), T066 (`electron-pressed-keys.spec.ts`; move the shared e2e helpers of `pressed-keys.spec.ts` into `tests/e2e/helpers/pressed-keys.ts` first), T064 (constitution-auditor), T065 (full gate); tree clean after this commit.

## 2026-09-25 13:05 - claude-sonnet-5 (implement, polish and final gate)
- Done: T062 (real files), T063 (quickstart), T064 (constitution review), T066 (Electron), T065 (full gate). All 58 tasks are ticked (53 planned + T067, T068 + T057-style additions counted in tasks.md).
- T062 real files, pictures looked at: `us1-real-chopin.png`, `us2-real-chopin.png` (OpenScore grand staff, G major: green heads, hollow half note stays a hollow green outline; F#4 / F#1 with no sign, A#3 with a sharp, low key on ledger lines), `real-debussy.png` (second real grand-staff piece: A#/Db disc with a sharp and "8vb" under the bass staff), library items with key signatures `real-cminor.png` (Chopin op.28 no.20, C minor = flat key + minor: B natural with a natural sign, Db with a flat, Bb with none), `real-eminor.png`, `real-gmajor.png`.
- T063: the quickstart's automated checks are inside the full gate below; its manual steps were done by pictures (US1 steps 1-4: `us1.png`, `us1-chord.png`, hollow head in `us1-real-chopin.png`; US2 steps 1-7: `us2-d5.png`, `us2-two.png`, `us2-sharp.png`, `us2-a0.png`, marks layer off in the e2e; US3: `us3-grace.png`, `us3-heldover.png`, `us3-skipped.png`, `us3-play.png`, `greyscale.png`).
- T064 constitution review (`constitution-auditor` subagent over `e266c27...HEAD`): verdict COMPLIANT WITH NOTES, no CRITICAL/HIGH. Findings and what was done: (MEDIUM, VI) a correct note differs from an unplayed one by colour only - owner-accepted in the spec, but an owner decision does not amend the constitution: recorded in plan Complexity Tracking as raised by the audit; **needs owner: accept that row, or approve a PATCH clarification of principle VI, or add a non-colour mark to correct notes**. (MEDIUM, gates) the Electron e2e failures: fixed `library.spec.ts` (own `--user-data-dir`, the single-instance-lock race), see below. (LOW) disc ellipse / serif ottava label added to Complexity Tracking; `band!` comment, guarded `closest()` cast, named `DISC_COLUMN_SPREAD_HEADS` in the view. Left as is, noted: `notehead` glyph harvested but unused (the test T030 requires it; drawing the disc from it is possible later), duplicated colour literals canvas vs `tokens.css`, chevron proportions inline in `pressed-keys.ts`, all three wrong states share one disc look (the reason stays on the on-screen keyboard), per-frame DOM reads in `drawDiscs` (measured: ten discs cost no more than idle frames), no automated ui-imports rule in `tests/architecture`.
- RT review: not applicable, no AudioWorklet, scheduler, MIDI timing or plugin code changed (plan "Real-time Paths Touched: none"; the auditor agreed); I did not run `rt-audio-reviewer`.
- T066: `tests/e2e/electron-pressed-keys.spec.ts` runs the US1 and US2 cases against the desktop shell under `app://` with the same assertions (passed). The shared helpers moved to `tests/e2e/helpers/pressed-keys.ts`.
- Full gate, final: `pnpm lint` exit 0 (284 warnings, 13 infos, unchanged from before the feature); `pnpm typecheck` exit 0; `pnpm test` Test Files 195 passed (195), Tests 2277 passed (2277), exit 0; `pnpm test:e2e` (chromium, firefox, webkit, electron): 361 passed, 90 skipped, **1 failed**: `[electron] electron-playback.spec.ts:47` "the audio clock advances under app://" received "stalled" (its own throw-away `AudioContext` did not advance in 300 ms). It passed in isolation earlier (2 passed) and `pnpm test:e2e --project=electron` alone passed twice in a row, 100 passed each (including this test and `library.spec.ts:175`). It failed in two of the last three full four-project runs, so it is load-sensitive: not green on a busy machine. It tests the desktop shell's own audio start, which this feature does not touch; treat it as a known flake, not an accepted deviation - say if it should be made robust (e.g. wait for the context to run) in a separate task.
- Earlier finding fixed: `library.spec.ts:175` launched Electron without its own user-data dir, so the shell's single-instance lock made it quit whenever another Electron spec ran (failed in every full run before; now passes in the full run and alone).
- Owner questions still open: (1) other hand's written note played: green `playedAlong` (implemented) vs red disc (spec line 138); (2) the principle-VI row above; (3) held-over key release clears its mark (my reading of FR-009a).
- Handoff: feature 008 is ready to merge except the open owner questions; not merged, not pushed. Tree clean at the commit below; run `git log --oneline main..HEAD`.

## 2026-09-25 13:40 - claude-opus-5.5 (owner answers)
- Owner: "use recommendations (answers only)". Recorded in spec Clarifications; docs only, no code or test changed.
- (1) Other hand's written note played while practising hands separately: green `playedAlong`, no disc (as implemented); spec edge case "Hands separately" corrected to match FR-004 and the 002 contract.
- (2) Principle VI (correct vs not-yet-played by colour only): plan Complexity Tracking row accepted; no constitution change.
- (3) Releasing a held-over key clears its orange mark with its hint (T068, as implemented); noted in research R-12.
- Not decided here: the load-sensitive `electron-playback.spec.ts:47` flake stays a known issue outside this feature (no task created).
- Handoff: feature 008 has no open owner questions; ready to merge when the owner asks (not merged, not pushed). Tree clean after this commit.

## 2026-09-25 14:10 - claude-sonnet-5 (continue: owner answers applied, last flake fixed)
- Session start: status showed NEXT STEP done; another agent (claude-opus-5.5) had already recorded the owner's "use recommendations" for questions 1-3 (`2d0dfea`); nothing left to apply there. The input "answered" carried no further content, so I read it as the same "use recommendations" and applied the one recommendation still open: question 4.
- Question 4 (recommendation: make the flaky test robust): `tests/e2e/electron-playback.spec.ts` no longer judges the throw-away `AudioContext` after 300 ms; it resumes the context and gives the clock up to 3 s to move. Test-only change, no product code.
- Evidence: `pnpm test:e2e` (all four projects): **362 passed, 90 skipped, 0 failed** (4.7 min) - first fully green e2e run of this feature, including `electron-playback` and `library.spec.ts:175`; `pnpm lint` exit 0 (284 warnings, 13 infos, unchanged). No source or unit test changed since the last `pnpm test` (Tests 2277 passed) and `pnpm typecheck` (exit 0) runs.
- Handoff: feature 008 is complete, gate green, no open owner questions; ready to merge when the owner asks (not merged, not pushed). Tree clean after this commit.
