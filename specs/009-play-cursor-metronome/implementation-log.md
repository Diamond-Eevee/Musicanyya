# Implementation log: 009-play-cursor-metronome

## 2026-09-25 18:15 - claude-opus-5.5 (analyze)
- Analyze: 16 findings (CRITICAL 1, HIGH 1, MEDIUM 7, LOW 7); tasks.md as of 3c4c3af
- Top recommendations: (C1) the skip icon sits in the old chevron box just below its notehead, where the next chord
  tone a third below is printed - it covers a written note (Constitution VI, FR-016, FR-026; Practice has the same
  defect since 008): place it clear of every head in its column (e.g. below the lowest head of the chord on that
  staff) and test chords a second and a third apart; (H1) add a test that no disc, skip icon or Grade mark is drawn
  during a live run (FR-027); (M1-M3) extend click tests to tempo/meter changes, anacrusis and 50/150 % (SC-002),
  cursor removal on stop and mode change (FR-006), and an automated no-overlap check over the library (SC-007)
- Handoff: next = resolve C1 in plan/contract/tasks (`/speckit.plan` edit or manual), then `/speckit.implement` from T001

## 2026-09-25 18:22 - claude-opus-5.5 (analyze remediation)
- Done: all 16 analyze findings resolved with the recommended option (owner). C1: skip icon below the lowest head of
  its column on that staff, both modes (spec FR-016/FR-016a, research R-13, contract `skipIconBox`); H1: T035 asserts
  no Grade mark during a live run; M1 T020 covers tempo/meter change, anacrusis, repeat, 50/150 %; M2 T010(c) stop and
  mode change; M3 new T054 (`e2e-synthetic-grade` seam) + T055 overlap sweep over the library; M4 `disc` ref explains
  all its refs; M5 SC-005 wording; M6 new T053 golden render before T022; M7 `caretBox` clear of accidentals; L1-L6
  folded into T031, T036, T034, T046, T047, T052; L7 unchanged (guard test by design).
- Handoff: next = `/speckit.implement` from T001 (US1 and US2 can run as parallel lanes); tree clean after this commit

## 2026-09-25 19:05 - claude-sonnet-5 (implement: Setup, Foundational, US1 tests)
- Session start: baseline before any change: `pnpm test` Test Files 196 passed, Tests 2286 passed; `pnpm lint` exit 0 (289 warnings, 13 infos, as before); `pnpm typecheck` exit 0. It matched the last hand-off.
- Done: T001-T011. T001 `MAX_SETUP_CONTROLLERS` + 001 constants table. T002 fixture `grade/grade-marks.musicxml` (opens with an empty load report, 6 passes: measure 1 twice). T003/T004 `sleep:<ms>` (parser, e2e `pressKeys`, screenshot `pressKeys`); `tests/tools/key-steps.test.ts` Tests 18 passed. T005 `tests/e2e/helpers/play.ts` (`startPlay`, `waitForGrade`, `playPhase`); proven by running T011's spec (it reaches the count-in and asserts on it). T006 `--run` / `--grade` in `tools/dev/screenshot.ts`, README, reference R7. T007 below.
- T007 "before" picture (`tests/.generated/009/t007-before.png`, `pnpm screenshot --item repertoire/beginner/fur-elise-theme-16-bar --run --keys "sleep:3500"`): the status bar reads "Play · Count-in · Measure 1", the first five measures are shown, and there is NO cursor bar and no highlighted note anywhere. Notices: none, console errors: none. This is today's behaviour (B-4).
- Tests written first (US1), each run and seen to fail for the expected reason:
  - T008 `tests/core/timeline/position.test.ts` + `listen-cursor-reference.ts` + `__snapshots__/listen-cursor.golden.json` (generated from the verbatim inline expressions of `updateCursor`, five files: the new fixture, the chord fixture, three real pieces): fails, module `position.js` missing.
  - T009 `tests/core/play/cursor.test.ts`: fails, module `cursor.js` missing.
  - T010 `tests/ui/score-view-play-cursor.test.ts` (new shared harness `tests/ui/helpers/score-view-harness.ts` + `recording-canvas.ts`: a real fixture Score, fake pages with `g.note > g.notehead`, fake geometry, recording canvas): `Tests 10 failed | 1 passed (11)`; (a)-(e) fail (no cursor in Play), (f) Listen passes as specified.
  - T011 `tests/e2e/play-cursor.spec.ts` (chromium): `3 failed, 1 passed`: the cursor-order, range-5-8 and green-under-highlight tests fail (no `.playing` in Play); the "gone after the Grade" test passes today only because nothing is highlighted yet.
- Decisions: `notesAtTick` / `passAtTick` take the compact `TimelineDto` shape the view holds (passes carry `endTick`), not core's `PlaybackTimeline` (passes carry `lengthTicks`): the view never has the latter and the golden must compare with the verbatim inline code. Contract 1.0.0 -> 1.0.1 (signature type only), recorded with T012.
- Problems / open questions: none. `pnpm test -- <path>` runs the whole suite (the `--` is passed through); use `pnpm vitest run <path>` for a single file.
- Handoff: next = T012 -> T013 -> T014 -> T015 (US1 implementation), then T016, T017; then US2 (T018-T027, T053), US3; tree clean at the commit after this entry

## 2026-09-25 19:40 - claude-sonnet-5 (implement: US1 checkpoint)
- Done: T012-T017 (US1). `src/core/timeline/position.ts` (`notesAtTick`, `passAtTick`), `src/core/play/cursor.ts` (`playCursorAt`); `mx-score-view.ts`: Listen uses `notesAtTick`/`passAtTick`; the Play cursor is drawn every frame (bar at the first note due, `.playing` on the notes due, none in the count-in), frame order clear -> cursor -> Grade marks, `followPlayCursor` reuses the same position, leaving Play for Listen or Practice clears highlights, marks and canvas. Contracts: play-run.md 1.2.0 (cursor part), play-display.md 1.0.1 (structural timeline type, see the earlier decision).
- Evidence: T008 golden and T009: `pnpm vitest run tests/core/timeline/position.test.ts tests/core/play/cursor.test.ts` Tests 27 passed. T010: `tests/ui/score-view-play-cursor.test.ts` 11 passed (was 10 failed | 1 passed). Checkpoint runs: `pnpm vitest run tests/core/timeline tests/core/play tests/ui` Test Files 69 passed, Tests 552 passed; full `pnpm vitest run` Test Files 199 passed, Tests 2333 passed; `pnpm typecheck` exit 0; `pnpm lint` exit 0, 289 warnings (unchanged). T011: `pnpm exec playwright test tests/e2e/play-cursor.spec.ts --project=chromium` 4 passed (was 3 failed); the existing Play/Practice specs (`us1-play`, `us2-grade`, `us3-play-setup`, `us3-run-chrome`, `us4-attempts`, `us4-overlays`, `us1-practice`, chromium) 17 passed.
- Independent Test, looked at `tests/.generated/009/t017-cursor.png` (`--run --keys "sleep:5200"`): the orange cursor bar with its dot at the top runs through measure 1 of the treble and bass staff at the first note, that note is highlighted (blue), the status bar reads "Play · Measure 1", no notices, no console errors. During the count-in (T007 picture, before the change) there was nothing. Not checked here: firefox and the Electron shell (T046 covers Electron).
- Decisions: the highlight set is kept in the view's existing `soundingNoteIds` (shared by Listen and Play), so leaving Play clears it through the same path. Firefox not run in this session.
- Problems / open questions: none.
- Handoff: next = US2: T053 (commit BEFORE T022), T018-T021 tests, then T022 -> T027; US3 after. Tree clean at the commit after this entry.

## 2026-09-25 20:40 - claude-sonnet-5 (implement: US2 checkpoint, the Metronome)
- Done: T018-T027, T053, T056, T057, T058 (US2). Two tasks were ADDED because the tests found real defects: T056 (a Score without `<time>` made `compilePlaySchedule` loop forever: Play on such a file froze the tab) and T057/T058 (research B-9/R-14: the Play schedule clicked only the COUNT-IN, so the click stopped when the run started; 003 FR-004 and 009 FR-009 to FR-011 always required the whole run). Research (B-9, R-14, R-02), spec "Changes from planning", plan and tasks were updated first.
- Changes: `src/engine/worklets/score-player.processor.ts` (channel setup stored on `schedule`, applied in the message handler once the sound is ready via `soundReady()`; optional synth port `programChange`/`setDrums`, mapped by the wrapper), `src/core/schedule/play-schedule.ts` (run clicks for every pass in range in the meter in force, pickup on its own beat; count-in without `<time>` terminates), `src/core/play/metronome.ts` + `defaults.ts` (`METRONOME_VOLUME_ON = 100`, `METRONOME_VOLUME_MUTED = 0`), `src/app/play-session.ts` and `src/app/session.ts` (always set the Metronome level). Contracts: worklet-protocol 1.4.0, play-run 1.2.0 (cursor + Metronome parts).
- Test changes because behaviour changed (each with the FR): `tests/core/play/play-schedule.test.ts` pickup test now counts the count-in clicks only (`tick < countInTicks`), because the run's own clicks follow them (FR-009: whole run); the new tests (a)-(i) cover the run (each assertion new, nothing loosened). `tests/engine/listen-render-golden.test.ts` was reshaped after T022 (see Problems).
- Evidence (summary lines): T018 guard `tests/core/schedule/setup-events.test.ts` 1 passed (over 150 fixtures and every library item, compiled with both compilers: no program/controller event after tick 0; it passes on old and new code as designed). T019 `tests/engine/worklets/score-player.setup.test.ts` 9 passed (was 7 failed: no `soundReady`). T020 `tests/engine/metronome-click.test.ts` 10 passed: real SpessaSynth and GeneralUser GS at 48 kHz, 7 Scores (100 %, tempo change and meter change at 50 % and 150 %, anacrusis, repeat): exactly one click per count-in and run beat (run beats counted from the written meters), each starts within 3 ms, peaks within 10 ms, tail energy below 0.01, downbeat above every beat click, every beat click at least a mezzo-forte piano C4, silence between clicks, more than 20 tails measured. On today's processor the same test failed with attack 33.8 ms ("expected 0.0337 to be <= 0.01"). T021 `tests/engine/play-session.test.ts` 19 passed (was 1 failed: an unmuted start sent no volume). T053 golden recorded on today's processor and committed first (`test(engine): golden fingerprint ...`), see Problems. T056 test hung until the 40 s timeout before the fix; T057 was 5 failed before the fix; then `pnpm vitest run tests/core/play tests/core/schedule` Tests 57 passed. Meter-in-force mutation check: with the fix neutralised, test (i) fails.
- T025 (real files): `mozart-quartet-k387.mxl` Listen, first 10 s: channels 0, 3, 6, 10 get programs 40, 40, 41, 42 (`programChange` calls recorded), 197 of 200 windows non-silent, the sound differs from the old (no-programs) render (max window RMS difference 0.038). `repertoire/beginner/ode-to-joy`: program 0 applied, shape difference vs the T053 golden 5.2e-8 (limit 1e-6), level ratio 1.0351.
- T026 RT review (`rt-audio-reviewer`, run as an agent): `process()` and `dispatchBlock` gained nothing; setup is reachable only from `receiveMessage` and `soundReady()`; ordering, bounds and Constitution II hold. Findings and what was done: HIGH `setChannelVolume(METRONOME_CHANNEL, 1)` is 1 % on the port's 0..100 scale (the plan's own "1", and the same wrong value already in `session.ts`'s live mute handler since 003) -> FIXED with `metronomeChannelVolume()` (0 / 100) in both places, tests, contract, research; MEDIUM a throw from `setDrums`/`programChange` left the other channels unconfigured and the `soundReady` status unsent -> FIXED (per-channel try/catch, one error, test (h)); LOW a malformed schedule replaced `schedule` before failing -> FIXED (setup stored first, test (i)); LOW CC0 bank select on drum channels -> no longer sent for drum channels (the real-synth click test still passes); LOW `heldNotes` is a `Set` on the RT path (an existing allocation risk, now hit once per beat by the clicks) -> NOT changed, out of scope, raised as a follow-up; LOW/advisory setup cost in the message handler -> not measured, cheaper than `addSoundBank`.
- Full gate after the fixes: `pnpm vitest run` Test Files 203 passed, Tests 2369 passed; `pnpm typecheck` exit 0; `pnpm lint` exit 0, 289 warnings (unchanged); chromium e2e (`us1-play us2-listen us2-grade us3-play-setup us3-run-chrome us4-attempts us4-overlays us1-practice play-cursor`) 23 passed. Screenshot `tests/.generated/009/t027-run.png` (`--run` on `learning/chords/c-major-scale-and-chords`, 6.5 s in): cursor bar, highlighted notes, "Play · Measure 1", no notices, no console errors.
- Decisions: the "on" level is 100 (see the RT finding). Run clicks use the meter in force: the Score model keeps `time` only where a `<time>` is written, so a local `withMeterInForce` in `play-schedule.ts` fills it in; this also fixes the count-in of a range that starts in a measure without its own `<time>`. NOT changed (out of scope, noted): Play grading windows (`beatTicksAt` users elsewhere) read `measures[i].time` without inheriting, so a 6/8 piece's later measures use the 4/4 default beat there.
- Problems / open questions:
  - needs owner: the "piano Scores sound as before" assumption (spec Assumptions, R-01) holds in notes, timing and timbre to 1e-6, but NOT in level: the processor now applies the Score's own `<volume>` and `<pan>` (001 research maps them to CC7 and CC10; they were silently dropped before). `ode-to-joy` writes `<volume>80</volume>` = CC7 102 against the synth default 100, so it is 3.5 % louder (+0.3 dB). Recommendation: accept (it is the 001 design and imperceptible); the alternative is to apply only drum flag, bank and program. The golden test now asserts the shape strictly and bounds the level (at most 5 % louder).
  - needs owner: the SC-004 listening check (the owner hears a metronome and tells the accented first beat from the others without looking). I cannot hear; the render test proves the click is a short percussive sound (peak within 10 ms, decays by 300 ms, louder than a piano mf, downbeat louder than the beats).
  - observation (Listen behaviour, unchanged): the cursor bar stands at the first due note in span order. With a whole-measure left-hand chord the bar stays at the chord while the right-hand notes light up (see `t027-run.png`). It is Listen's existing rule; say if the Play bar should follow the latest onset instead.
  - `pnpm test -- <path>` runs the whole suite; use `pnpm vitest run <path>`.
- Handoff: next = US3 (T028-T045, T054, T055). Start with the tests T028-T036 (all different files), then T037-T044; T054/T055 before T039/T040/T043 are finished. T046-T052 after. Tree clean at the commit after this entry.

## 2026-09-25 22:10 - claude-sonnet-5 (implement: US3 checkpoint, the Grade in Practice's look)
- Done: T028-T045, T054, T055 (US3). The Grade is drawn as Practice draws: green head = correct, grey head + skip icon = missed (and a wrong pitch's own note), red-orange disc at the pitch played in the column of its note = wrong pitch / extra key, `<` / `>` caret beside the head = late / early; no ring, cross or outline anywhere. Core: `placeKeys` (`src/core/notation/place-discs.ts`), `gradeMarks(score, grade, passes)` with `contexts` (`src/core/grade/marks.ts`), `syntheticLog` (`src/core/grade/synthetic.ts`). UI: `disc-layout.ts` (`skipIconBox`, `caretBox`), `pressed-keys.ts` (`drawStateChevron` skipped = skip icon), `note-index.ts`, `grade-marks.ts` (`GradeMarkGeometry`, `drawGradeMarks`, `discAt`), `mx-score-view.ts` (geometry measured once per `domEpoch`, content coordinates, disc-first click, `revealSelectedMark`), `playState` (`marks`, `selectedMark`), `mistake-stepper`, `mx-grade-panel`, `reason-text`, `session.ts` (`showGrade`, `e2e-synthetic-grade`). Contracts: 008 pressed-keys 2.1.0, 003 play-run (Grade wording), 009 play-display 1.1.0, 009 data-model, research R-08 findings.
- Test changes because behaviour changed (each with its FR): the old Grade ring/cross/diamond assertions were replaced by assertions on the new marks (FR-014/FR-019): `tests/ui/grade-marks.test.ts` (rewritten, T031), `tests/ui/overlay-layers.test.ts` and `tests/ui/practice-marks.test.ts` (skipped notes are one icon per column and staff, FR-016a; a chevron below one head no longer exists), `tests/ui/pressed-keys.test.ts` and `tests/ui/disc-layout.test.ts` (skip icon box, FR-016a/FR-026), `tests/ui/mistake-stepper.test.ts` (rewritten: refs, extras included, FR-023), `tests/ui/grade-panel.test.ts` (`selectMark`, FR-022a), `tests/ui/score-view-play-cursor.test.ts` case (e) (marks instead of rings), `tests/e2e/us2-grade.spec.ts` (`selectedMark`), `tests/ui/no-dashed-lines.test.ts` (comment only). The 003 grading goldens are untouched.
- Evidence (summary lines): T028 `place-keys.test.ts`, T029 `marks.test.ts` (31 tests + snapshot) and `marks-real.test.ts` (76 tests on every real score), T030 `synthetic.test.ts`, T031-T035 the ui tests above; `pnpm vitest run` Test Files 208 passed | 1 failed, Tests 2547 passed | 1 failed - the one failure is `tests/files/mxl.test.ts` "throws on zip bomb over MAX_UNCOMPRESSED_BYTES", a 5 s timeout under full-suite load (unchanged code, feature 001); `pnpm vitest run tests/files/mxl.test.ts` alone: Tests 7 passed (raised as a follow-up). `pnpm typecheck` exit 0; `pnpm lint` exit 0, 284 warnings (was 289, not higher). T036 `pnpm exec playwright test tests/e2e/play-grade-marks.spec.ts --project=chromium --project=firefox`: 4 passed (2 tests x 2 engines). T054/T055: `pnpm exec playwright test tests/e2e/grade-marks-overlap.spec.ts --project=chromium`: 58 passed (4.7 m) - the fixture and all 57 library items, `nothing` and `semitoneHigh` Grades, every scroll position: no skip icon or caret covers a head, accidental or dot. Mutation checks: 6 mutations of the `gradeMarks` rules were each caught; a mutated `skipIconBox` made the sweep fail.
- T045 Independent Test, looked at `tests/.generated/009/t045-zoom2.png` (fur-elise-theme-16-bar, right hand, measures 1-2, built app, keys timed on the run clock): green heads for the three correct notes, a caret `<` (yellow) after the late D#5 and `>` (blue) before the early E5, a grey head with the skip icon and an orange D#4 disc with its sharp for the D#5 played an octave low, a grey head with skip icon for the missed E5 and the missed D5, and an orange C5 disc over the D5 column for the extra key. Grade results: `[correct onTime, correct late, correct early, wrongPitch, missed, correct onTime, missed]`, discs `[63, 72]`. Note: the same keys against `vite`'s dev server shifted by about a note (all marks still correct for what was graded); the e2e and this picture use the built app. Note: `pnpm screenshot --run --keys` has no way to time keys on the run clock (`sleep:` starts at the Play click, the count-in length varies), so the picture was made by `tests/.generated/t045-shot.ts` (a scratch script, not committed) with `pressInTime`; a screenshot with a first guess of the count-in graded every note missed and showed all grey heads with skip icons and no console error.
- Deviations from the plan (all recorded in the contract, data-model and research R-08): `gradeMarks` takes the passes of the run's passage (the extra key's column is chosen by tick, which the Grade does not carry); a disc whose key equals a correct head's key in its column is not drawn (it would hide the green head); 003 grades only an octave error as `wrongPitch`, any other wrong key is a missed note plus an extra key; T054's own unit test is `tests/core/grade/synthetic.test.ts`.
- Latent 003 problems found, not fixed (out of scope): played-along spans on repeats; `beatTicksAt` does not inherit the meter; `heldNotes` allocates a Set (RT review). Recorded here as follow-ups for the owner.
- Handoff: next = T046 (Electron), T047 (frame rate), T048 (greyscale), T049, T050, T051, T052. Tree clean at the commit after this entry.
