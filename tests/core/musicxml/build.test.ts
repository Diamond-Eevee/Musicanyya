import { describe, it, expect } from 'vitest';
import { buildScore } from '../../../src/core/musicxml/build.js';

describe('buildScore', () => {
  it('builds time-model with file snapshots', () => {
    expect(true).toBe(false); // TODO: implement snapshot generation
  });
});
