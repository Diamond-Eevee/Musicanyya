import { describe, expect, it } from 'vitest';
import { fromMusicXml } from '../../../tools/library/fidelity/from-musicxml';

describe('fromMusicXml', () => {
  it('merges tied notes into one', () => {
    expect(true).toBe(false);
  });

  it('keeps grace notes apart', () => {
    expect(true).toBe(false);
  });

  it('uses sounding pitch for octave shifts', () => {
    expect(true).toBe(false);
  });

  it('handles tuplet triplet eighths as exact thirds', () => {
    expect(true).toBe(false);
  });

  it('handles repeatStart, repeatEnd, and endings per bar', () => {
    expect(true).toBe(false);
  });

  it('handles pickup bars with short length and printed number 0', () => {
    expect(true).toBe(false);
  });

  it('keeps spelling', () => {
    expect(true).toBe(false);
  });

  it('gets played order from buildTimeline', () => {
    expect(true).toBe(false);
  });
});
