import { describe, expect, it } from 'vitest';
import { loadRecords } from '../../../tools/library/fidelity/records';

describe('loadRecords', () => {
  it('validates audit-record schema', () => {
    expect(true).toBe(false);
  });
  it('re-run count must equal expectedDifferences', () => {
    expect(true).toBe(false);
  });
  it('differenceNotes count must match', () => {
    expect(true).toBe(false);
  });
  it('visual-only check can never report "mechanical"', () => {
    expect(true).toBe(false);
  });
  it('relabelled requires a changed claim', () => {
    expect(true).toBe(false);
  });
  it('removed requires a README row', () => {
    expect(true).toBe(false);
  });
  it('reviewedBy/reviewedOn must equal the record', () => {
    expect(true).toBe(false);
  });
  it('unknown source fails', () => {
    expect(true).toBe(false);
  });
});
