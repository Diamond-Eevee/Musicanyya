import { describe, expect, it } from 'vitest';
import { theoryCheck } from '../../../tools/library/fidelity/theory';

describe('theoryCheck', () => {
  it('checks C major scale', () => {
    expect(true).toBe(false);
  });
  it('checks a minor ii-V-i', () => {
    expect(true).toBe(false);
  });
  it('checks a major I-vi-IV-V', () => {
    expect(true).toBe(false);
  });
  it('checks a dominant seventh chord', () => {
    expect(true).toBe(false);
  });
});
