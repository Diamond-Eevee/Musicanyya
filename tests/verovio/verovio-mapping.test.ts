import { describe, it, expect } from 'vitest';

describe('Verovio mapping test', () => {
  it('maps printed Note ID and Measure ID to SVG element ids (g.note, g.measure)', () => {
    expect(false).toBe(true);
  });
});
